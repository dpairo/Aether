server:
  port: 8080

spring:
  application:
    name: Aether
  datasource:
    url: jdbc:h2:mem:devdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    com.aether.app: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
      probes:
        enabled: true

# External API Configuration
air-quality:
  waqi:
    base-url: "http://api.waqi.info"
    token: "${WAQI_TOKEN:demo}"  # Use environment variable or demo token
    timeout: 5000
  cities:
    spain:
      - name: "Madrid"
        lat: 40.4168
        lon: -3.7038
        city-id: "madrid"
      - name: "Barcelona"
        lat: 41.3851
        lon: 2.1734
        city-id: "barcelona"
      - name: "Valencia"
        lat: 39.4699
        lon: -0.3763
        city-id: "valencia"
      - name: "Sevilla"
        lat: 37.3891
        lon: -5.9845
        city-id: "sevilla"
      - name: "Bilbao"
        lat: 43.2627
        lon: -2.9253
        city-id: "bilbao"
      - name: "Zaragoza"
        lat: 41.6488
        lon: -0.8891
        city-id: "zaragoza"

# Strava OAuth Configuration
strava:
  client-id: "${STRAVA_CLIENT_ID:179600}"
  client-secret: "${STRAVA_CLIENT_SECRET:d411c16fb8c1d70f8d1fb95e080185c7f0c36985}"
  redirect-uri: "${STRAVA_REDIRECT_URI:http://localhost:8080/api/v1/strava/auth/callback}"
  auth-url: "https://www.strava.com/oauth/authorize"
  token-url: "https://www.strava.com/oauth/token"