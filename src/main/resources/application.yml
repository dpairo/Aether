server:
  port: 8080

spring:
  application:
    name: test_hackathon
  datasource:
    url: jdbc:h2:mem:devdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    java.test_hackathon.app: DEBUG